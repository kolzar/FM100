<UserControl x:Class="FM100.Views.MatchSimulationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Background="{DynamicResource PrimaryBackgroundBrush}"
        FontFamily="Segoe UI">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2d2d2d" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Home Club -->
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock x:Name="HomeClubName" Text="Home Club" FontSize="18" FontWeight="Bold" Foreground="#00d4ff"/>
                    <TextBlock x:Name="HomeScore" Text="0" FontSize="14" Foreground="#888888"/>
                </StackPanel>

                <!-- VS -->
                <TextBlock Grid.Column="1" Text="VS" FontSize="20" FontWeight="Bold" Foreground="#666666" 
                          VerticalAlignment="Center" Margin="20,0"/>

                <!-- Away Club -->
                <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <TextBlock x:Name="AwayClubName" Text="Away Club" FontSize="18" FontWeight="Bold" Foreground="#00d4ff"/>
                    <TextBlock x:Name="AwayScore" Text="0" FontSize="14" Foreground="#888888"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Match Info -->
        <Border Grid.Row="1" Background="#1e1e1e" Padding="15" BorderBrush="#333333" BorderThickness="0,1,0,1">
            <TextBlock x:Name="MatchTimeText" Text="0'" FontSize="12" Foreground="#888888"/>
        </Border>

        <!-- Events List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="EventsList" Background="#1e1e1e" Padding="15">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="#00d4ff" Margin="0,5" FontSize="12"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="3" Background="#2d2d2d" Padding="15"><StackPanel Orientation="Horizontal" HorizontalAlignment="Right"><Button x:Name="FastForwardButton" Content="⏭️ FAST FORWARD" Height="35" Width="120" FontSize="11" FontWeight="Bold" Background="#4a9eff" Foreground="White" Cursor="Hand" Margin="0,0,10,0"/><Button x:Name="EndMatchButton" Content="🏁 END MATCH" Height="35" Width="100" FontSize="11" FontWeight="Bold" Background="#00d4ff" Foreground="Black" Cursor="Hand"/></StackPanel></Border>
    </Grid>
</UserControl>
